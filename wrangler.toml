name = "ghl-metrics"
main = "src/index.js"
compatibility_date = "2026-01-30"

kv_namespaces = [
  { binding = "METRICS_KV", id = "PASTE_REAL_KV_NAMESPACE_ID_HERE" }
]

[vars]
# DO NOT put secrets here if you can avoid it.
# Better: set WORKER_INGEST_TOKEN as a Secret in Cloudflare dashboard.
